// let {padName, padNumeric} = require('../index.js');
//
// console.log(padName((3, 'vzxc'),('abcde')));
//
// // curry((w, s) => pipe(split("."), map(padNumeric(w)), join("."))(s))
//
// // let pad = curry((z, w, s) => {
// //         return (z.repeat(w) + s).slice(s.length)
// // })
